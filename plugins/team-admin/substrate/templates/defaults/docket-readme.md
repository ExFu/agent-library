# Docket

What is open here: tasks (`todo.jsonl`), reminders (`reminders.jsonl`), and things left for your agents to attend to (`agent-backlog.jsonl`), plus the files that say when and how they get heard. One line per entry; your librarians keep the files small.
